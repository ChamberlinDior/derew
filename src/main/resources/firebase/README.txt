# Place your Firebase service-account.json here
# Download from: Firebase Console → Project Settings → Service accounts → Generate new private key
# Then set oviro.fcm.enabled=true in application.yml
